func add(a, b) => a + b
func min(a, b) => a - b
func mul(a, b) => a * b
func div(a, b) => a / b
func mod(a, b) => a % b
func pow(a, b) => a ^ b

func max(list) {
    val last_item = 0s
    val current_item = 0
    val tmp = []
    val length = len(list)

    for i=0 to length then
        val current_item = list : i
        if current_item > last_item then
            append(tmp, current_item)
            val last_item = current_item
        end
    end

    ret tmp : (len(tmp) - 1)
}

func min(list) {
    val last_item = MATH_POS_INF
    val current_item = 0
    val tmp = []
    val length = len(list)

    for i=0 to length then
        val current_item = list : i
        if current_item < last_item then
            append(tmp, current_item)
            val last_item = current_item
        end
    end

    ret tmp : (len(tmp) - 1)
}
